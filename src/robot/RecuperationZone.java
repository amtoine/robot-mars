package robot;

public class RecuperationZone {
	double x;
	double y;
	double d;
	
	public static void goTo() {
		//rotate until sees recuperation zone (knowing its position in map and robot pose and heading
		//rotate again until min distance to recuperation zone is minimum
		//travel forward
	}
}
